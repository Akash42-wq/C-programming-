#include <stdio.h>

int main(){
    int i,n, factorial;
    factorial = 1;
    printf("Enter the number : ");
    scanf("%d", &n);
    for(i=n; i>=1; i--){
        factorial = factorial * i;
    }
    printf("Factorial : %d", factorial);
}
